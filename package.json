{
  "name": "nba-central",
  "version": "1.0.0",
  "description": "Sitio de noticias, estadisticas y cuotas de NBA generado estaticamente",
  "type": "module",
  "private": true,
  "scripts": {
    "fetch-data": "node scripts/fetch-data.js",
    "generate": "node scripts/generate-site.js",
    "build": "npm run fetch-data && npm run generate"
  },
  "dependencies": {
    "nunjucks": "^3.2.4",
    "rss-parser": "^3.13.0"
  },
  "engines": {
    "node": ">=18"
  }
}
